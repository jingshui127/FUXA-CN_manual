# 🎨 FUXA 组件开发教程

---

## 📋 概述

FUXA 组件（Widgets）是基于 **SVG** 和 **JavaScript** 的自定义可视化元素，允许您创建完全个性化的界面组件。

### 核心概念

- **SVG**：可缩放矢量图形，用于绘制组件外观
- **JavaScript**：用于实现组件的交互逻辑
- **数据绑定**：将组件与 FUXA 标签关联，实现数据交互

---

## 🎯 组件基本结构

FUXA 组件使用纯 SVG 和 JavaScript，通过 script 标签实现功能：

```html
<svg>
  <!-- SVG 内容在这里 -->
  <rect x="10" y="10" width="100" height="50" fill="blue" />

  <!-- JavaScript 脚本在这里 -->
  <script>
    // JS 脚本内容
  </script>
</svg>
```

---

## 📤 数据传递机制

### 1️⃣ 从组件发送数据到 FUXA

使用 `postValue` 函数将值从 SVG 发送到 FUXA：

```javascript
function postValue(id, value) {
  console.error('Not defined!');
}
```

**使用方法**：
```javascript
// 调用函数，ID 必须与变量名完全匹配
postValue('_pn_value', someNewValue);
```

### 2️⃣ 从 FUXA 接收数据到组件

使用 `putValue` 函数从 FUXA 接收值到 SVG：

```javascript
function putValue(id, value) {
  if (id === '_pn_value') {
    callFunction(value);
    newVar = value;
  }
}
```

**工作原理**：
- 等待 FUXA 调用该函数
- 检查 ID 是否匹配我们需要的变量名
- 执行相应的处理逻辑

---

## 🔢 参数类型

FUXA 支持多种参数类型，用于在 SVG 脚本和 FUXA 之间传递数据：

| 参数类型 | 前缀 | 说明 | 示例 |
|---------|------|------|------|
| **布尔型** | `_pb_` | true 或 false | `_pb_enabled` |
| **数值型** | `_pn_` | Int, Float, Real 等 | `_pn_value` |
| **字符串型** | `_ps_` | 字符串 | `_ps_text` |
| **颜色型** | `_pc_` | 十六进制颜色代码 | `_pc_color` |

### 示例

```javascript
//!export-start
let _pb_enabled = true;      // 布尔型参数
let _pn_value = 50;          // 数值型参数
let _ps_text = "Hello";      // 字符串型参数
let _pc_color = "#00ff00ff"; // 颜色型参数
//!export-end
```

---

## 📦 变量导出

### 导出注释

变量必须包含在 `//!export-start` 和 `//!export-end` 注释之间：

```javascript
//!export-start
let _pn_value = 50;
//!export-end
```

### 在 FUXA 中使用

导出后，变量将在 FUXA 的 SVG 属性面板中可用，您可以在那里绑定 FUXA 标签。

---

## ⚠️ 重要注意事项

### 1️⃣ setInterval 清理

如果您使用 `setInterval`，必须在切换页面或进入编辑器时清除间隔，否则组件会因为另一个 `setInterval` 被调用而损坏。

### 2️⃣ 检查 SVG 是否存在

在执行任何操作前，检查 SVG 对象是否存在：

```javascript
const checkDestroy = document.getElementById('svgIdName'); // 重要：必须是 SVG 的名称！
if (!checkDestroy) {
  clearBlinking();
  return;
}
```

### 3️⃣ 使用 MutationObserver

使用 MutationObserver 监控 SVG 元素的移除：

```javascript
// 设置 MutationObserver 监听 SVG 元素的移除
const observer = new MutationObserver((mutations) => {
  mutations.forEach((mutation) => {
    mutation.removedNodes.forEach((node) => {
      if (node.id === 'svgIdName') { // 重要：必须是 SVG 的名称！
        clearBlinking();
        observer.disconnect();
      }
    });
  });
});

// 开始观察 body 或 SVG 的父元素
observer.observe(document.body, { childList: true, subtree: true });
```

### 4️⃣ SVG ID 设置

确保在 SVG 文件的顶部使用 SVG ID：

```html
<svg id="svgIdName" width="200" height="100">
  <!-- SVG 内容 -->
</svg>
```

---

## 🎨 完整示例

### 示例 1：简单的温度计组件

```html
<svg id="tempGauge" width="200" height="300" xmlns="http://www.w3.org/2000/svg">
  <!-- 温度计背景 -->
  <rect x="50" y="20" width="100" height="260" fill="#f0f0f0" stroke="#333" stroke-width="2" rx="10" />

  <!-- 温度计刻度 -->
  <g id="scale">
    <!-- 刻度线将通过 JavaScript 动态生成 -->
  </g>

  <!-- 温度值显示 -->
  <text id="tempValue" x="100" y="280" text-anchor="middle" font-size="24" fill="#333">
    0°C
  </text>

  <script>
    //!export-start
    let _pn_temperature = 0;  // 温度值
    let _pn_minTemp = 0;      // 最小温度
    let _pn_maxTemp = 100;    // 最大温度
    let _pc_color = "#ff0000"; // 温度计颜色
    //!export-end

    // 初始化
    function init() {
      updateGauge();
    }

    // 更新温度计
    function updateGauge() {
      const temp = _pn_temperature;
      const min = _pn_minTemp;
      const max = _pn_maxTemp;

      // 计算温度百分比
      const percentage = (temp - min) / (max - min);
      const height = percentage * 220; // 220 是可用高度

      // 更新温度值显示
      document.getElementById('tempValue').textContent = temp + '°C';

      // 绘制温度条
      let existingBar = document.getElementById('tempBar');
      if (existingBar) {
        existingBar.remove();
      }

      const bar = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
      bar.setAttribute('id', 'tempBar');
      bar.setAttribute('x', '60');
      bar.setAttribute('y', 260 - height);
      bar.setAttribute('width', '80');
      bar.setAttribute('height', height);
      bar.setAttribute('fill', _pc_color);
      bar.setAttribute('rx', '5');

      document.getElementById('tempGauge').appendChild(bar);
    }

    // 接收 FUXA 数据
    function putValue(id, value) {
      if (id === '_pn_temperature') {
        _pn_temperature = value;
        updateGauge();
      }
    }

    // 初始化
    init();

    // 清理函数
    function clearBlinking() {
      // 清理定时器等资源
    }

    // MutationObserver
    const checkDestroy = document.getElementById('tempGauge');
    if (!checkDestroy) {
      clearBlinking();
      return;
    }

    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        mutation.removedNodes.forEach((node) => {
          if (node.id === 'tempGauge') {
            clearBlinking();
            observer.disconnect();
          }
        });
      });
    });

    observer.observe(document.body, { childList: true, subtree: true });
  </script>
</svg>
```

### 示例 2：带动画的指示灯

```html
<svg id="indicator" width="100" height="100" xmlns="http://www.w3.org/2000/svg">
  <!-- 指示灯圆形 -->
  <circle id="led" cx="50" cy="50" r="40" fill="#ccc" stroke="#333" stroke-width="2" />

  <!-- 文字标签 -->
  <text id="label" x="50" y="90" text-anchor="middle" font-size="12" fill="#333">
    状态
  </text>

  <script>
    //!export-start
    let _pb_status = false;  // 状态（开/关）
    let _pc_onColor = "#00ff00";  // 开启颜色
    let _pc_offColor = "#ff0000"; // 关闭颜色
    let _ps_label = "状态";       // 标签文字
    //!export-end

    let blinkInterval = null;

    // 更新指示灯
    function updateIndicator() {
      const led = document.getElementById('led');
      const label = document.getElementById('label');

      // 更新标签文字
      label.textContent = _ps_label;

      // 更新颜色
      if (_pb_status) {
        led.setAttribute('fill', _pc_onColor);
      } else {
        led.setAttribute('fill', _pc_offColor);
      }
    }

    // 接收 FUXA 数据
    function putValue(id, value) {
      if (id === '_pb_status') {
        _pb_status = value;
        updateIndicator();
      } else if (id === '_ps_label') {
        _ps_label = value;
        updateIndicator();
      }
    }

    // 初始化
    updateIndicator();

    // 清理函数
    function clearBlinking() {
      if (blinkInterval) {
        clearInterval(blinkInterval);
        blinkInterval = null;
      }
    }

    // MutationObserver
    const checkDestroy = document.getElementById('indicator');
    if (!checkDestroy) {
      clearBlinking();
      return;
    }

    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        mutation.removedNodes.forEach((node) => {
          if (node.id === 'indicator') {
            clearBlinking();
            observer.disconnect();
          }
        });
      });
    });

    observer.observe(document.body, { childList: true, subtree: true });
  </script>
</svg>
```

---

## 🎭 高级功能

### 1️⃣ 使用 CSS 样式

您可以在 SVG 中使用 CSS 来美化组件：

```html
<svg id="myWidget" width="200" height="200" xmlns="http://www.w3.org/2000/svg">
  <style>
    .button {
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .button:hover {
      opacity: 0.8;
    }
    .button:active {
      transform: scale(0.95);
    }
  </style>

  <rect id="btn" class="button" x="50" y="50" width="100" height="50" fill="#4CAF50" rx="5" />
  <text id="btnText" x="100" y="80" text-anchor="middle" fill="white" font-size="16">
    点击
  </text>

  <script>
    const btn = document.getElementById('btn');
    btn.addEventListener('click', function() {
      postValue('_pn_clickCount', 1);
    });
  </script>
</svg>
```

### 2️⃣ 访问 DOM 元素

使用标准的 `getElementById` 和 `addEventListener` 访问和操作元素：

```javascript
// 获取元素
const element = document.getElementById('elementId');

// 添加事件监听
element.addEventListener('click', function() {
  // 处理点击事件
});

// 修改属性
element.setAttribute('fill', '#ff0000');

// 修改样式
element.style.opacity = '0.5';
```

### 3️⃣ 动画效果

使用 JavaScript 创建动画效果：

```javascript
// 简单的闪烁动画
let blinkState = false;
setInterval(function() {
  blinkState = !blinkState;
  const led = document.getElementById('led');
  led.setAttribute('fill', blinkState ? '#00ff00' : '#003300');
}, 500);

// 平滑过渡
function animateValue(start, end, duration) {
  const startTime = performance.now();

  function update(currentTime) {
    const elapsed = currentTime - startTime;
    const progress = Math.min(elapsed / duration, 1);

    const value = start + (end - start) * progress;
    updateDisplay(value);

    if (progress < 1) {
      requestAnimationFrame(update);
    }
  }

  requestAnimationFrame(update);
}
```

---

## 📚 更多示例

查看 FUXA GitHub 仓库中的完整示例：

🔗 **组件示例库**：https://github.com/frangoteam/FUXA/tree/master/server/_widgets

### 可用的示例组件

- **按钮**：各种样式的按钮
- **仪表盘**：圆形和线性仪表盘
- **图表**：实时数据图表
- **指示灯**：各种状态的指示灯
- **开关**：切换开关
- **滑块**：数值滑块
- **进度条**：进度指示器
- **文本框**：输入和显示文本

---

## 💡 最佳实践

### 1️⃣ 命名规范

使用清晰的命名规范：

```javascript
// 参数命名
let _pn_temperature = 0;    // 温度值
let _pb_enabled = true;     // 启用状态
let _ps_label = "温度";     // 标签文字
let _pc_color = "#ff0000";  // 颜色

// 函数命名
function updateGauge() { }      // 更新仪表盘
function clearBlinking() { }   // 清除闪烁
function handleData() { }      // 处理数据
```

### 2️⃣ 资源管理

确保正确管理资源：

```javascript
// 在组件销毁时清理资源
function clearBlinking() {
  // 清除定时器
  if (blinkInterval) {
    clearInterval(blinkInterval);
    blinkInterval = null;
  }

  // 移除事件监听器
  if (element) {
    element.removeEventListener('click', handleClick);
  }

  // 清理其他资源
}
```

### 3️⃣ 错误处理

添加错误处理机制：

```javascript
try {
  // 可能出错的代码
  updateGauge();
} catch (error) {
  console.error('组件错误:', error);
  // 显示错误信息
  showError(error.message);
}
```

### 4️⃣ 性能优化

优化组件性能：

```javascript
// 使用 requestAnimationFrame 进行动画
requestAnimationFrame(animate);

// 避免频繁的 DOM 操作
// 批量更新
function updateMultipleElements() {
  const updates = [
    { id: 'elem1', attr: 'fill', value: '#ff0000' },
    { id: 'elem2', attr: 'opacity', value: '0.5' }
  ];

  updates.forEach(update => {
    const elem = document.getElementById(update.id);
    if (elem) {
      elem.setAttribute(update.attr, update.value);
    }
  });
}

// 使用缓存
let cachedElement = null;
function getCachedElement(id) {
  if (!cachedElement) {
    cachedElement = document.getElementById(id);
  }
  return cachedElement;
}
```

---

## ❓ 常见问题

### ❓ 组件不显示

**可能原因**：
1. SVG ID 未设置
2. 组件尺寸设置不正确
3. 样式冲突

**解决方案**：
1. 确保 SVG 有正确的 ID
2. 检查 width 和 height 属性
3. 检查 CSS 样式

### ❓ 数据不更新

**可能原因**：
1. `putValue` 函数未正确实现
2. 变量名不匹配
3. 数据绑定未设置

**解决方案**：
1. 检查 `putValue` 函数实现
2. 确保变量名与 FUXA 中的绑定一致
3. 在 FUXA 中正确绑定标签

### ❓ 内存泄漏

**可能原因**：
1. 定时器未清除
2. 事件监听器未移除
3. MutationObserver 未断开

**解决方案**：
1. 在 `clearBlinking` 中清除所有定时器
2. 移除所有事件监听器
3. 断开 MutationObserver

---

## 🎉 总结

通过本教程，您已经学会了：

✅ FUXA 组件的基本结构  
✅ 数据传递机制（postValue 和 putValue）  
✅ 参数类型和变量导出  
✅ 使用 MutationObserver 管理组件生命周期  
✅ 创建自定义组件  
✅ 使用 CSS 和 JavaScript 增强组件功能  
✅ 最佳实践和性能优化  

现在您可以创建自己的个性化组件了！

---

> 💡 **提示**：查看 FUXA 官方组件库获取更多灵感和示例。

---

**关注我们，获取更多工业自动化技术干货！** 🎯

<div style="display: flex; justify-content: center; align-items: center; gap: 40px; margin: 30px 0;">
    <div style="text-align: center;">
        <img src="images/微信公众号.jpg" alt="微信公众号" style="width: 200px; height: 200px; object-fit: contain;">
        <p style="margin: 10px 0 0 0; font-size: 14px; color: #666;">微信公众号</p>
    </div>
    <div style="text-align: center;">
        <img src="images/单人二维码.png" alt="技术支持" style="width: 200px; height: 200px; object-fit: contain;">
        <p style="margin: 10px 0 0 0; font-size: 14px; color: #666;">技术支持</p>
    </div>
</div>

